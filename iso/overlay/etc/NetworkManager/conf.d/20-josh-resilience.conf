# Josh OS connection resilience.
# NetworkManager's global autoconnect retry default is normally finite.
# Zero means profiles that use the default keep retrying indefinitely.
[main]
autoconnect-retries-default=0
